replicaCount: 1
autoscaling:
  enabled: false

backend:
  name: backend
  group: api
  image:
    repository: docker.io/jvikstedt/gke-demo-app
    pullPolicy: IfNotPresent
    tag: "0.1"
  service:
    name: backend-service
    type: NodePort
    port: 3000
    targetPort: 3000
